@model MvcCarManagerment.Models.ManagerModel
@using MvcCarManagerment.Service
@{
    ViewBag.Title = "后台信息发布";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
@using (Html.BeginForm("Manager", "ManagerMent", FormMethod.Post, new { enctype = "multipart/form-data" }))
{  
    @Html.ValidationSummary(true)
    @Html.HiddenFor(entity => entity.ID)
    <table>
        <tr>
            <td width="15%" valign="middle">
                文章标题 <span style="color: Red; margin: 0,0,-10px,0;">*</span>
            </td>
            <td>
                @Html.TextBoxFor(entity => entity.Title, new { @class = "acticle_header" })
                @Html.ValidationMessageFor(entity => entity.Title)
            </td>
        </tr>
        <tr>
            <td width="20%" align="center" valign="middle">@Html.LabelFor(entity => entity.Type)
            </td>
            <td>@Html.DropDownListFor(entity => entity.TypeID, new SelectList(Basicinfo.GeTypeList(), "ID", "Name", Basicinfo.GetType(@ViewBag.tag)))
            </td>
        </tr>
        <tr>
            <td width="20%" align="center" valign="middle">
                图片路径
            </td>
            <td>
                <input type="file" id="fileName" name="fileName" width="300px" />
                &nbsp; <span style="color: Red; line-height: 27px; height: 27px;">添加图片新闻时必须添加图片</span>
            </td>
        </tr>
    </table>
  
@*  <textarea id="editor" name="editor"></textarea>*@
    @Html.TextAreaFor(entity => entity.NewsComment)
      
        
    <div>
        <input type="submit" value="保存" id="save" />
    </div>
}
@section scripts{
    @Scripts.Render("~/Areas/Admin/Scripts/ueditor/ueditor.all.js")
    @Scripts.Render("~/Areas/Admin/Scripts/ueditor/ueditor.all.min.js")
    @Scripts.Render("~/Areas/Admin/Scripts/ueditor/ueditor.config.js")
    @Styles.Render("~/Areas/Admin/Scripts/ueditor/themes/iframe.css")
    <script type="text/javascript">

        $(function () {
            var editor = new baidu.editor.ui.Editor({
                UEDITOR_HOME_URL: '/Areas/Admin/Scripts/ueditor/', //配置编辑器路径
                iframeCssUrl: '/Areas/Admin/Scripts/ueditor/themes/iframe.css', //样式路径
                initialContent: '', //初始化编辑器内容
                autoHeightEnabled: false, //高度自动增长
                minFrameHeight: 500, //最小高度
                initialFrameHeight: 580//初始化编辑器高度
            });
            editor.render('NewsComment');
        });

    </script>
}
